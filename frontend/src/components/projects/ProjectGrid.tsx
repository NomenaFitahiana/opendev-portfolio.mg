/**
 * Grille de projets
 * Affichage responsive des cartes de projets
 */

export default function ProjectGrid({ projects }: any) {
  return (
    <div className="project-grid">
      {/* Grille responsive de ProjectCard */}
      {/* Support mobile, tablette, desktop */}
    </div>
  )
}
