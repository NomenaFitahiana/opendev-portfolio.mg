/**
 * Indicateur de chargement
 * Spinner animé pendant les requêtes
 */

export default function LoadingSpinner() {
  return (
    <div className="loading-spinner">
      {/* Animation de chargement */}
    </div>
  )
}
